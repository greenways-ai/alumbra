# Stale mesh rejection

This complete Showcase project captures the AR-03 revision fence. Mesh work for canonical revisions one and two completes out of order. Revision two installs; the later result for revision one is counted and discarded before it can replace current render state.

The story publishes only bounded revision and counter evidence. It contains no test callback, mesh buffer, worker handle, runtime object or executable fixture source.
