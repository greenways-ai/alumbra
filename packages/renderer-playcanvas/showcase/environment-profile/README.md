# Environment profiles

This complete renderer Showcase applies the installed daylight, fog and emissive-night profiles to the same bounded material scene. An unknown material profile is also requested through the real prebuilt renderer and rejected before any mesh or material resource is allocated.

Only profile identities, counts, fog mode, rejection evidence and disposal baseline are projected. PlayCanvas objects and renderer authority stay inside the host.
